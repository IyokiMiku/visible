# 待完成清单

## P0：仍需处理

### 1. 补齐正式映射表

- [x] 确认 `04_生成输出/生产规划/重庆市 电子信息类/` 下是否缺少映射表
- [x] 从规划表生成正式映射表（L3/L2优先，禁止L1根节点）
- [x] 校验映射表覆盖率（AI生成/聚合卷不再误报）
- [x] 映射表已可正常使用

### 2. 统一题型命名

- [x] 建立 `question_types.py` 单一事实源（TYPE_NORMALIZE + TYPE_ORDER + TYPE_DISPLAY）
- [x] `paper_assembler.py` 已接入
- [x] `runner.py` 已接入（取代 _BLUEPRINT_TO_CODE_TYPE / _QUESTION_TYPE_ORDER 等散落映射）
- [x] 综合题/综合分析题/综合应用题统一归一化

### 3. 运行状态预览

- [x] 选卷号前显示已生成/待生成及区间格式（`_print_generation_status`）
- [x] 缺失卷号用压缩区间展示（如 "2-28, 30-93"）

### 4. 映射表覆盖检查修复

- [x] AI生成/聚合卷不再被误报为"映射表未覆盖"
- [x] 检查 A列实际存在性，不依赖 B列是否有值

---

## P0：仍需继续

### 5. 验证 API 拉题字段链路

- [x] 系统记录每卷 courseId/typeId/kpointIds 的获取情况
- [x] 记录 API 返回中哪些字段会缺失或变化
- [x] 验收标准：拉题阶段不会因字段缺失中断整卷流程

### 6. 加模式 2 启动前校验

- [x] 在 `create.py` 或 `runner.py` 加正式映射表存在性检查
- [x] 缺少关键文件时直接报错并提示补什么
- [x] 验收标准：模式 2 入口能提前拦截配置问题

### 7. 修正文档命令

- [x] 检查 `README.md` 中是否仍引用根目录不存在的 `planning.py`
- [x] 改成仓库里真实存在的命令或入口

---

## P1：降低人工操作量

### 9. 结构化人工审核队列

- [ ] 错误收集.md 已有基础，可升级为 `review_queue.json`
- [ ] 记录卷号、路径、问题题号、类型、建议动作
- [ ] 验收标准：人工审核项可集中查看流转

### 10. 结构化落盘 API / 修复结果

- [x] 落盘 API 缺题信息、补题结果、修复前后对比
- [x] 验收标准：单卷问题能回溯，不靠控制台输出

---

## P2：清理冗余

### 11. 更新架构说明和移植版说明

- [x] 重写或标注"可复用代码与架构说明"为历史文档
- [x] 更新 `01_工具脚本/学科网API拉题移植版/README.md`
- [x] 说明哪些函数已接入主流程，哪些仍是调试工具
- [x] 验收标准：文档与代码状态一致

---

## 注意

- 考点训练卷的细目表不需要生成（代码从题型汇总自动生成虚拟细目表）
- 专题训练卷和课程综合卷需要细目表——目前重庆市电子信息类尚未生成
- 知识点题目数量文档已生成（电工/电子/单片机），可用于窄考点判断

