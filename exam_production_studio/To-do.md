# To-do

## 通用

- [x] 1. 可以在题型那里添加自定义题型（名词解释）
- [x] 2. 给界面加信息保留，别换界面之后就消失了
  - 项目创建界面：草稿保留（sessionStorage），切走再回来内容不丢，创建成功后自动清空
  - 产品系列专属配置界面：编写说明/编写规范「改过才自动保存」——切换产品系列前、跳去别的页面前自动落库；关闭/刷新标签页有未保存改动时弹原生提醒；样张与当前编写说明不一致时显示黄色提示（不阻拦操作）；预览仅在「刷新预览」「保存编写说明」时重新生成
- [ ] 3. 想一下输出逻辑是什么，改成自动生成到本地某个文件夹，加 fix 功能
- [ ] 4. 增加项目完成的记忆系统
- [ ] 5. 改一课一练和考点双析卷的编写规范
- [x] 6. 全局设置提前，在首次运行的时候弹出，不填满必须的内容不允许保存，temperature 设置范围 0.01-0.3
- [x] 7. 加一个 cookie 自动读取
- [ ] 8. 一课一练不需要分值，其他的分值检测是否为 100（根据试题类型设定加在产品系列设置里）
- [ ] 9. 必写的内容没写完不允许保存项目
- [ ] 10. 识别那里用视觉模型自己扫描然后给人工判断作为规划表生成依据（或者 RAG 切片，总之就是调 LLM+？？？）

## 资源导入界面

- [ ] 1. 在项目创建完成后，改成在界面上方加横向进度条，不在左侧边栏显示，边栏应该显示不同项目供选择，切换项目时保留项目进度显示
- [ ] 2. 支持文件拖动、只能上传 PDF 及 word 文档
- [ ] 3. 把英文改成中文
- [ ] 4. 模板加默认，规划表也加默认使用生成的，做选项
- [ ] 5. 文件类型支持与否

## 流程执行界面

- [ ] 1. 待确认界面说人话，让人能看懂
- [ ] 2. 在工作的话要显示目前的工作状态，不能让用户错认我们正在进行的项目停止了
- [ ] 3. 工作中不允许点继续
- [ ] 4. 状态改成中文
- [ ] 5. 上面的进度？

## 人工内容审阅工作流（通用第 3 条细化 · 路线B）

> 原则：题目落 JSON、质检不通过才暂停人工审阅、审阅只改内容不改格式、逐题按题号升序确认全部通过后才格式装配。每步只做一件事，禁止一步做过量。

### A. 质检升级（结构化 + 补规则）
- [x] A1. 把质检问题从扁平字符串改为结构化：{scope(单题/全卷/跨卷), question_no, type, severity, detail}；原有 5 条规则改成结构化输出
- [x] A2. 移植「废选项」检查（正常/无影响等填充词）
- [x] A3. 移植「答案自暴露」检查（题干含正确选项较长片段）
- [x] A4. 移植「解析过短」检查
- [x] A5. 移植「单选答案分布失衡」（全卷级）
- [x] A6. 移植「多选全选 ABCD + 多选答案分布」（全卷级）
- [x] A7. 移植「同卷题干查重」（全卷级）
- [x] A8. 质检报告 report.py 适配结构化问题（分单题/全卷两块输出）
- [x] A9. 引入 AI 深度质检：判断整卷内容与主题/知识点的相符程度，产出结构化问题（归入整卷级），供审阅页展示

### A（修订 · 对齐考纲百套卷新质检规则）
- [x] A10. QCIssue 增加稳定 code 字段（供审阅页把问题类型绑定修复动作）
- [x] A11. 新增单题规则：缺题干 / 判断题答案格式(√×) / 选项缺失 / 选项结构不一致 / 解析缺计算过程 / 难度名称不规范 / 网页残留 / 缺必要配图；并加 skip_analysis_checks（主观题答案≥10字或含图时跳过解析类检查）降误报
- [x] A12. 阈值对齐考纲：解析过短≤10、选项长度失衡加 max_len>5 护栏、废选项词表扩充、单选答案分布 >40% 判严重且某项<15%(总数≥8)判严重、模糊查重阈值 0.60 / 0.90
- [x] A13. 整卷：题量与规划不符（以 meta.needed 合计为期望）；题干重复拆「精确重复=严重 / 模糊疑似=警告」两级

### 后续增强（暂缓，另行排期）
- [ ] 跨卷检查：答案泄露、知识点密度过高、公式指纹去重、图片指纹去重、数字替换题识别（依赖 image_refs/sha256/kpointIds 等 studio 暂无字段）
- [ ] LaTeX / 格式残留清理检查：需先确保不误伤 studio 保留的 $...$ / {{math:}} 公式源码
- [ ] 题型错误校验（对细目表逐题期望题型）：依赖逐题蓝图，studio 现无

### B. 图片预下载前移
- [x] B1. 在 pull.produce_questions 里对每题 stem_images/option_images 预下载到本地，补 local_path
- [x] B2. assemble/generator 改为复用已下载图片（幂等，无需改）

### C. 题目持久化（JSON）
- [x] C1. 定义题目 JSON 结构（Question 全字段 + 图片 local_path）与读写模块
- [x] C2. produce_questions 后写入 04_生成输出/_题目数据/第N卷.json
- [x] C3. 质检结果（结构化）写入 第N卷_质检.json

### D. 流程改序 + 审阅闸门（质检不过才暂停）
- [x] D1. 各类型 flow_nodes 在「质检导出」后、「组卷」前插入「内容审阅」「格式装配」节点
- [x] D2. 逐卷循环拆分：拉题→存题目JSON→质检→存质检JSON
- [x] D3. 质检通过的卷：直接进入格式装配；质检不通过的卷：登记 CONTENT_REVIEW 并暂停
- [x] D4. 把 assemble（格式装配）移到「审阅通过之后」执行
- [x] D5. resume/装配时从题目 JSON 重新加载（读人工改后的内容）
- [x] D6. 审阅状态：每卷「待审/已通过」+ 逐题确认进度（confirmed_nos），落 JSON

### E. 审阅后端接口
- [x] E1. GET 某卷题目 + 结构化质检问题（列表 + 详情）
- [x] E2. 本地图片静态服务接口（按 local_path 提供）
- [x] E3. PUT 保存单题文本编辑（题干/选项/答案/解析），改动后自动取消确认
- [x] E4. POST 换单选/多选选项顺序（自动同步答案字母 + 选项图片）
- [x] E5. POST AI 重生成单题（沿用原 prompt，固定题型/题号/知识点，仅重出题干+选项+答案+解析）
- [x] E6. POST 单题「确认」
- [x] E7. POST 整卷「通过」（全部题确认后才可调用；关闭该卷待确认项，全部处理完自动 resume 触发装配）

### F. 审阅前端页面
- [x] F1. 新页面路由（/projects/:id/content-review）+ 侧栏入口 + 待审卷列表（状态+问题计数+确认进度）
- [x] F2. 逐题卡片渲染：题干/选项/答案/解析，按题号从小到大依次显示
- [x] F3. 公式用 KaTeX 渲染（services/math.ts）；图片按题干/选项位置显示
- [x] F4. 单题质检问题挂到对应题卡；整卷/跨卷类问题单独区展示（仅提示不阻拦）
- [x] F5. 文本编辑 + 公式源码编辑与实时预览
- [x] F6. 换选项顺序交互（上下移，答案字母联动）
- [x] F7. AI 重生成单题按钮 + loading + 结果替换
- [x] F8. 逐题「确认」勾选；全部确认后「整卷通过」按钮才可用（人工改后不再自动质检）

### G. 归档增强
- [x] G1. 原卷版生成时给主观题插入空白作答区（每题后 4 行）
- [x] G2. 归档时「解析版+原卷版」配对打包 zip
- [x] G3. 归档时分类到 解析版/原卷版/压缩包 子目录
