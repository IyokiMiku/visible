# To-do

## 通用

- [ ] 1. 规划表读的卷数不对
- [ ] 2. 规划表生成流程建立
- [ ] 3. OCR 扫描后需要有人工复核的一步
- [ ] 4. 分清楚必须和不必须的文件，上传后有明显的显示
- [ ] 5. 改一课一练和考点双析卷的编写规范（考点双析卷已完成，并对齐卷首版式 + 配套改代码/前端；一课一练待改：§十一/§十二 标题需改为权威三级版式「省份（考试类型）一课一练 / 《教材》（出版社·版次）第 x 练 / 第 X 单元 一级标题 第 Z 章 二级标题 第 W 节 主题」；且要真正生成出该版式还依赖规划表链路对齐，详见 `Q：规划表生成.md`）
- [ ] 6. 识别哪里用视觉模型自己扫描然后给人工判断作为规划表生成依据（或者 RAG 切片，总之就是调 LLM + ???）
- [ ] 7. AI 匹配映射表有问题
- [ ] 8. 确认课程量（规划表读取，可人工修改）

## 资源导入界面

- [ ] 1. 在项目创建完成后，改成在界面上方加横向进度条，不在左侧边栏显示；边栏应该显示不同项目供选择，切换项目时保留项目进度显示
- [ ] 2. 支持文件拖动、只能上传 PDF 及 Word 文档
- [ ] 3. 把英文改成中文
- [ ] 4. 模板加默认，规划表也加默认使用生成的，做选项
- [ ] 5. 文件类型支持与否

## 流程执行界面

- [ ] 1. 待确认界面说人话，让人能看懂
- [ ] 2. 在工作的话要显示目前的工作状态，不能让用户错认我们正在进行的项目停止了
- [ ] 3. 工作中不允许点继续
- [ ] 4. 状态改成中文
- [ ] 5. 上面的进度？

## 协作一致性（多人分支合并后需统一）

- [ ] 1. **状态文案统一**：同一英文状态在不同页面显示成了不同中文，需统一为一套术语。
  - `ProjectList.vue`（项目状态）：`running → 生成中`、`review → 待审核`
  - `Flow.vue`（流程状态，来自 Gloria 改动）：`running → 执行中`、`review → 等待人工确认`
  - 建议抽一份公共状态字典，或明确区分「项目状态」与「流程状态」两套术语并各自固定文案。
- [ ] 2. **分支基线统一**：团队 PR 目标分支不一致（marex0 的 PR 目标是 `dev`，Gloria 的 PR 目标是 `main`），需统一合并策略，避免 `main` 与 `dev` 分叉。
- [ ] 3. **`Flow.vue` 的 `NODE_LABEL` 空转**：其键是英文（`planning/mapping/pull`），但后端 `flow_nodes` / `current_node` 返回的本就是中文，映射命不中、只是原样透传。需改为按真实节点名（中文）映射，或删除该无效映射。
- [ ] 4. **回退节点映射对不上**：`runner.py` 的 `_RERUN_*` 含 `assemble → 组卷生成`，但 `_NODE_KEY` 并无「组卷生成」键（现装配节点名为「格式装配」），从该节点回退会落到默认进度 0，需对齐节点命名。

## 质检升级

- [ ] 跨卷检查：答案泄露、知识点密度过高、公式指纹去重、图片指纹去重、数字替换题识别（依赖 image_refs/sha256/kpointIds 等 studio 暂无字段）
- [ ] LaTeX / 格式残留清理检查：需先确保不误伤 studio 保留的 `$...$` / `{{math:}}` 公式源码
- [ ] 题型错误校验（对细目表逐题期望题型）：依赖逐题蓝图，studio 现无
